VITE_API_BASE_URL=http://localhost:8000

# The single seeded MVP user's id (no auth yet — see backend/app/models.py).
# Confirmed against the dev DB during SCRUM-13. Fragile: if the dev DB is ever
# reset and the seeded user gets recreated, this id may change and would need
# updating here. Accepted limitation for the single-user MVP.
VITE_SEEDED_USER_ID=2
